import 'package:flutter_test/flutter_test.dart';
import 'package:oewang_native/main.dart';

void main() {
  testWidgets('App class exists', (WidgetTester tester) async {
    expect(OewangApp, isNotNull);
  });
}
